# 更多关于Release文件的规范和约定，请点击 https://yuque.antfin.com/aone/geh0qs/252891532

# 构建源码语言类型
code.language=go

# 构建使用的go语言版本
build.tools.go=1.20.9

# 构建使用的maven命令，当前支持maven命令，不写使用打包规范默命令认. 该项填写时，必须指定 build.output
build.command=build.sh

docker.repo=reg.docker.alibaba-inc.com/aone
docker.tag=${TIMESTAMP_US}_${CODE_BRANCH_COMMIT_ID}_${SCHEMA_NAME}